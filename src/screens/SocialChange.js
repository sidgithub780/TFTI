import { StyleSheet, Text, View } from "react-native";
import React from "react";

import Screen from "../components/Screen";

const SocialChange = () => {
  return (
    <Screen>
      <Text>SocialChange</Text>
    </Screen>
  );
};

export default SocialChange;

const styles = StyleSheet.create({});
