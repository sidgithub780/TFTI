import { StyleSheet, Text, View } from "react-native";
import React from "react";

const EventDescriptionChange = () => {
  return (
    <View>
      <Text>EventDescriptionChange</Text>
    </View>
  );
};

export default EventDescriptionChange;

const styles = StyleSheet.create({});
