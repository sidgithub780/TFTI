import { StyleSheet, Text, View } from "react-native";
import React from "react";

import Screen from "../components/Screen";

const NoteChange = () => {
  return (
    <Screen>
      <Text>NoteChange</Text>
    </Screen>
  );
};

export default NoteChange;

const styles = StyleSheet.create({});
