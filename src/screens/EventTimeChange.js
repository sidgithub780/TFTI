import { StyleSheet, Text, View } from "react-native";
import React from "react";

const EventTimeChange = () => {
  return (
    <View>
      <Text>EventTimeChange</Text>
    </View>
  );
};

export default EventTimeChange;

const styles = StyleSheet.create({});
